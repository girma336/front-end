"use client";

import React from "react";
import { SignupContainer, JuubixAppNames } from "@/shared";

const SignPage: React.FC = () => (
  <SignupContainer appName={JuubixAppNames.MainApp} />
);

export default SignPage;
